import{_ as o,H as t,f as s,i as a}from"./chunks/framework.BauLNg70.js";const u=JSON.parse('{"title":"Instructions for Claude (copy this file to the root of your mod)","description":"","frontmatter":{},"headers":[],"relativePath":"ai-friendly/CLAUDE.template.md","filePath":"ai-friendly/CLAUDE.template.md"}'),r={name:"ai-friendly/CLAUDE.template.md"};function d(i,e,n,c,l,h){return t(),s("div",null,[...e[0]||(e[0]=[a(`<h1 id="instructions-for-claude-copy-this-file-to-the-root-of-your-mod" tabindex="-1">Instructions for Claude (copy this file to the root of your mod) <a class="header-anchor" href="#instructions-for-claude-copy-this-file-to-the-root-of-your-mod" aria-label="Permalink to &quot;Instructions for Claude (copy this file to the root of your mod)&quot;">​</a></h1><blockquote><p>Copy this file to the root of your mod, renaming it to <code>CLAUDE.md</code>. Claude Code reads it automatically on startup.</p></blockquote><h2 id="what-this-project-is" tabindex="-1">What this project is <a class="header-anchor" href="#what-this-project-is" aria-label="Permalink to &quot;What this project is&quot;">​</a></h2><p>External mod for a game in the <strong>my-game-fw</strong> framework (Vue/TS/Quasar mini-games with a sandboxed mod system).</p><ul><li><strong>Target game</strong>: <code>&lt;gameId&gt;</code> (declared in <code>mod.json</code>, usually <code>snake-classic</code>).</li><li><strong>Preferred engine</strong>: <code>quickjs-declarative-ui</code> with fallback <code>quickjs</code>. The engine evaluates <code>dist/mod.js</code> in a WASM sandbox.</li><li><strong>What does NOT exist in the sandbox</strong>: <code>window</code>, <code>document</code>, <code>fetch</code>, <code>XMLHttpRequest</code>, <code>process</code>, <code>require</code>, <code>import</code>, <code>localStorage</code>, <code>sessionStorage</code>, DOM, file system.</li><li><strong>The only thing that exists</strong>: the global <code>host: ModHost</code> declared as ambient in <code>src/globals.d.ts</code>.</li></ul><h2 id="key-files" tabindex="-1">Key files <a class="header-anchor" href="#key-files" aria-label="Permalink to &quot;Key files&quot;">​</a></h2><table tabindex="0"><thead><tr><th>Path</th><th>Role</th></tr></thead><tbody><tr><td><code>mod.json</code></td><td>Mod manifest. <code>id</code>, <code>version</code>, <code>target</code>, permissions, metadata. <strong>Any API you use in code requires its permission here.</strong></td></tr><tr><td><code>src/index.ts</code></td><td>Entry point. Compiles to <code>dist/mod.js</code> (IIFE ES2020 via esbuild).</td></tr><tr><td><code>src/globals.d.ts</code></td><td>Declares <code>host: ModHost</code> as global ambient. <strong>NEVER import <code>host</code> from here or anywhere else</strong> — it&#39;s global.</td></tr><tr><td><code>src/types.ts</code></td><td>Local subset of HostBridge. Source of the types you see in <code>host.*</code>.</td></tr><tr><td><code>locales/en.json</code>, <code>locales/es.json</code></td><td>i18n. Keys with the namespace declared in <code>mod.json#permissions[type=i18n].namespaces</code>.</td></tr><tr><td><code>build.mjs</code></td><td>esbuild pipeline. Doesn&#39;t usually need editing.</td></tr><tr><td><code>package.json</code></td><td>Scripts <code>build</code>, <code>validate</code>, <code>pack</code>.</td></tr><tr><td><code>dist/mod.js</code></td><td>Build output. Gitignored.</td></tr></tbody></table><h2 id="hard-rules-—-claude-must-never-violate" tabindex="-1">Hard rules — Claude must NEVER violate <a class="header-anchor" href="#hard-rules-—-claude-must-never-violate" aria-label="Permalink to &quot;Hard rules — Claude must NEVER violate&quot;">​</a></h2><ol><li><strong>Do not import <code>host</code></strong>. It&#39;s global ambient, not a module. If you see <code>import { host } from ...</code>, it&#39;s a bug. Only <code>/// &lt;reference path=&quot;./globals.d.ts&quot; /&gt;</code> at the top of <code>index.ts</code>.</li><li><strong>Do not use <code>window</code>, <code>document</code>, <code>fetch</code>, <code>process</code>, <code>require</code>, dynamic <code>import</code>, <code>eval</code>, <code>Function</code> constructor, DOM, file system, <code>localStorage</code></strong>. The sandbox blocks all that. If you need network, use <code>host.http</code> with <code>network</code> permission declared. If you need persistence, use <code>host.storage</code>.</li><li><strong>Do not write outside of <code>host.storage</code></strong>. The quota is in <code>mod.json</code> (<code>permissions.storage.quotaKb</code>). Exceeding it rejects with <code>QUOTA_EXCEEDED</code>.</li><li><strong>Do not invent permissions without updating <code>mod.json</code></strong>. If you add <code>host.subscribeEvent(&#39;FOO&#39;, ...)</code> in code, also add <code>&#39;FOO&#39;</code> to the <code>subscribe[]</code> of the <code>events</code> permission. If you add <code>host.callHostFn(&#39;barFn&#39;, ...)</code>, also add the corresponding surface to the <code>game-specific</code> permission.</li><li><strong>Do not invent event or host-fn names</strong>. The source of truth is the target game&#39;s <code>api-reference.md</code>. Common Snake Classic events: <code>GAME_STARTED</code>, <code>GAME_OVER</code>, <code>SCORE_CHANGED</code>, <code>LEVEL_UP</code>, <code>POWER_UP_PICKED</code>, <code>POWER_UP_EXPIRED</code>. Host fns: <code>gameConfigSet</code>, <code>gameConfigReset</code>, <code>gameConfigSnapshot</code>, <code>togglePowerUp</code>, <code>setPowerUpSpawnChance</code>. <strong>Any other name, verify it in api-reference before using.</strong></li><li><strong>Do not mix <code>tunables.&lt;X&gt;</code> binding + a hook that writes the same key</strong>. It&#39;s a race condition (last-write-wins). Canonical pattern: the binding applies to the game live AND the hook reads from storage to re-apply on <code>GAME_STARTED</code>. DO NOT write the same path from two places.</li></ol><h2 id="soft-rules-—-conventions" tabindex="-1">Soft rules — conventions <a class="header-anchor" href="#soft-rules-—-conventions" aria-label="Permalink to &quot;Soft rules — conventions&quot;">​</a></h2><ul><li><strong><code>id</code></strong> in <code>mod.json</code>: convention <code>&lt;handle&gt;.&lt;modname&gt;</code> (kebab, lowercase, e.g. <code>yourhandle.power-mixer</code>). Immutable.</li><li><strong><code>version</code></strong>: strict SemVer. <code>patch</code> for bugfixes, <code>minor</code> for features, <code>major</code> breaks (resets player settings automatically).</li><li><strong><code>rationale</code></strong> in each permission: text the player reads. NOT trivial (&quot;for fun&quot; loses installs). Concrete: (&quot;Applies the initial speed the player picked in the slider&quot;).</li><li><strong>i18n keys</strong> start with the declared namespace: <code>mod.&lt;modId&gt;.tab.title</code>, <code>mod.&lt;modId&gt;.section.spawn</code>, etc. Out of namespace, the framework rejects.</li><li><strong>TypeScript strict</strong> with <code>isolatedModules</code>. If you touch HostBridge types, do it in <code>src/types.ts</code>, not in <code>globals.d.ts</code>.</li><li><strong>max-len 80</strong> in imports (split multi-line if they exceed).</li><li><strong>No obvious comments</strong>. Only comment the &quot;why&quot; when the &quot;what&quot; isn&#39;t trivial from code.</li></ul><h2 id="before-any-change" tabindex="-1">Before any change <a class="header-anchor" href="#before-any-change" aria-label="Permalink to &quot;Before any change&quot;">​</a></h2><ol><li><strong>Read full <code>mod.json</code></strong>. Knowing which permissions are declared avoids requesting APIs without permission.</li><li><strong>Read the relevant section of the target game&#39;s api-reference</strong> (<a href="https://leteoworks.github.io/mod-portal-snake-classic/api-reference" target="_blank" rel="noreferrer">https://leteoworks.github.io/mod-portal-snake-classic/api-reference</a>). Confirm the event / host fn you&#39;ll use EXISTS with that exact name.</li><li><strong>Read the code you&#39;ll touch</strong>. Before adding a toggle, read <code>src/index.ts</code> and <code>src/settings-tab.ts</code> (if it exists) to see the descriptor shape the mod already uses.</li></ol><h2 id="after-any-change" tabindex="-1">After any change <a class="header-anchor" href="#after-any-change" aria-label="Permalink to &quot;After any change&quot;">​</a></h2><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">pnpm</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> build</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">       # esbuild → dist/mod.js</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">pnpm</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> validate</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">    # zod check of manifest against the schema</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># Local sideload (path per OS):</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">#   macOS:   ~/Library/Application\\ Support/snake-classic/mods/&lt;modId&gt;/</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">#   Windows: %APPDATA%/snake-classic/mods/&lt;modId&gt;/</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">#   Linux:   ~/.config/snake-classic/mods/&lt;modId&gt;/</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># Restart the game. Activate the mod. Manual smoke test: the</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># behavior described in the commit/prompt works.</span></span></code></pre></div><p>If the change touches <code>mod.json</code>: re-run <code>pnpm validate</code>. If it touches locales: verify that the keys in code exist in both <code>en.json</code> and <code>es.json</code> locales.</p><h2 id="quick-pointers-—-when-you-touch-x-read-y" tabindex="-1">Quick pointers — &quot;when you touch X, read Y&quot; <a class="header-anchor" href="#quick-pointers-—-when-you-touch-x-read-y" aria-label="Permalink to &quot;Quick pointers — &quot;when you touch X, read Y&quot;&quot;">​</a></h2><table tabindex="0"><thead><tr><th>If you&#39;ll touch...</th><th>Read first</th></tr></thead><tbody><tr><td>Adding a new slider/toggle</td><td><a href="https://leteoworks.github.io/mod-portal-snake-classic/tutorial/02-slider-tunable" target="_blank" rel="noreferrer">tutorial/02-slider-tunable</a> — <code>tunables.</code> binding concept + last-write-wins anti-pattern.</td></tr><tr><td>Reacting to a game event</td><td><a href="https://leteoworks.github.io/mod-portal-snake-classic/tutorial/03-game-events" target="_blank" rel="noreferrer">tutorial/03-game-events</a> — events catalog + permissions.</td></tr><tr><td>Customizing power-ups (Snake)</td><td><a href="https://leteoworks.github.io/mod-portal-snake-classic/tutorial/04-power-ups" target="_blank" rel="noreferrer">tutorial/04-power-ups</a> — array→UI pattern + bulk presets with <code>Promise.all</code>.</td></tr><tr><td>Preparing release to Workshop</td><td><a href="https://leteoworks.github.io/mod-portal-snake-classic/tutorial/05-release-ready" target="_blank" rel="noreferrer">tutorial/05-release-ready</a> — i18n, icon, validate, pack, upload.</td></tr><tr><td>Solving a concrete problem</td><td><a href="https://leteoworks.github.io/mod-portal-snake-classic/cookbook" target="_blank" rel="noreferrer">cookbook</a> — 10 copy-paste recipes.</td></tr><tr><td>Something not working, don&#39;t know why</td><td><a href="https://leteoworks.github.io/mod-portal-snake-classic/troubleshooting" target="_blank" rel="noreferrer">troubleshooting</a> — symptom→fix table.</td></tr><tr><td>Detail of a permission or <code>mod.json</code> field</td><td><a href="https://leteoworks.github.io/mod-portal-snake-classic/manifest-format" target="_blank" rel="noreferrer">manifest-format</a>.</td></tr><tr><td>Knowing which <code>host.*</code> is available</td><td><a href="https://leteoworks.github.io/mod-portal-snake-classic/api-reference" target="_blank" rel="noreferrer">api-reference</a>.</td></tr><tr><td>Your mod should work in ≥1 framework game</td><td><a href="https://leteoworks.github.io/mod-portal-snake-classic/targeting-games" target="_blank" rel="noreferrer">targeting-games</a> — logical <code>gameId</code> vs AppID.</td></tr></tbody></table><h2 id="reference-mods-real-code-not-toy" tabindex="-1">Reference mods (real code, not toy) <a class="header-anchor" href="#reference-mods-real-code-not-toy" aria-label="Permalink to &quot;Reference mods (real code, not toy)&quot;">​</a></h2><p>When a prompt of yours matches something already done in production, look at the real code first:</p><ul><li><a href="https://github.com/leteoworks/my-game-fw-mods/tree/main/snake-classic/studio.fun-config" target="_blank" rel="noreferrer"><code>studio.fun-config</code></a> — 22 power-up toggles + 3 presets (Classic/Casual/Hardcore).</li><li><a href="https://github.com/leteoworks/my-game-fw-mods/tree/main/snake-classic/studio.gameplay-tuner" target="_blank" rel="noreferrer"><code>studio.gameplay-tuner</code></a> — quantitative sliders + Easy/Normal/Hard presets.</li></ul><p>Pasting a fragment of their code to Claude as a reference (&quot;do it like in studio.fun-config: array→UI pattern&quot;) tends to produce better results than describing the pattern in words.</p>`,22)])])}const g=o(r,[["render",d]]);export{u as __pageData,g as default};
