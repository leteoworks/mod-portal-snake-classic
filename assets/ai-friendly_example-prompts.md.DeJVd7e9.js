import{_ as a,H as n,f as e,i as t}from"./chunks/framework.BauLNg70.js";const g=JSON.parse('{"title":"Tested prompt gallery — vibe-coding your mod","description":"","frontmatter":{},"headers":[],"relativePath":"ai-friendly/example-prompts.md","filePath":"ai-friendly/example-prompts.md"}'),p={name:"ai-friendly/example-prompts.md"};function l(i,s,o,r,c,d){return n(),e("div",null,[...s[0]||(s[0]=[t(`<h1 id="tested-prompt-gallery-—-vibe-coding-your-mod" tabindex="-1">Tested prompt gallery — vibe-coding your mod <a class="header-anchor" href="#tested-prompt-gallery-—-vibe-coding-your-mod" aria-label="Permalink to &quot;Tested prompt gallery — vibe-coding your mod&quot;">​</a></h1><p>Each prompt is <strong>self-contained</strong>: you can copy-paste into Claude Code (CLI or IDE) and get a useful result. We assume your mod has <a href="./CLAUDE.template"><code>CLAUDE.md</code></a> at the root — that&#39;s what gives Claude context about the framework, sandbox, and mod files.</p><p>Three levels by risk:</p><ul><li><strong>Level 1</strong> — Mechanical (Claude does it alone, you accept if it builds).</li><li><strong>Level 2</strong> — Full simple mod (Claude proposes, you review manifest + permissions + event names).</li><li><strong>Level 3</strong> — Release boilerplate (text generated in parallel to your work).</li></ul><hr><h2 id="level-1-—-mechanical-tasks-safe-vibe-coding" tabindex="-1">Level 1 — Mechanical tasks (safe vibe-coding) <a class="header-anchor" href="#level-1-—-mechanical-tasks-safe-vibe-coding" aria-label="Permalink to &quot;Level 1 — Mechanical tasks (safe vibe-coding)&quot;">​</a></h2><h3 id="_1-1-—-add-a-new-toggle-to-an-existing-tab" tabindex="-1">1.1 — Add a new toggle to an existing tab <a class="header-anchor" href="#_1-1-—-add-a-new-toggle-to-an-existing-tab" aria-label="Permalink to &quot;1.1 — Add a new toggle to an existing tab&quot;">​</a></h3><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>Add a new toggle to the mod tab that controls whether game-over</span></span>
<span class="line"><span>notifications appear as a toast. Persist between sessions with</span></span>
<span class="line"><span>binding to the mod&#39;s storage.</span></span>
<span class="line"><span></span></span>
<span class="line"><span>Details:</span></span>
<span class="line"><span>- Label: &quot;Show toast at game over&quot;.</span></span>
<span class="line"><span>- Binding: &#39;showToastOnGameOver&#39; (no tunables. prefix — it&#39;s</span></span>
<span class="line"><span>  only mod storage, doesn&#39;t touch the game).</span></span>
<span class="line"><span>- Default: true.</span></span>
<span class="line"><span>- Add it to the &quot;Notifications&quot; card of the tab; create the card</span></span>
<span class="line"><span>  if it doesn&#39;t exist.</span></span>
<span class="line"><span></span></span>
<span class="line"><span>DO NOT add a new hook yet — I&#39;ll write the handler that reads</span></span>
<span class="line"><span>the toggle and shows the toast.</span></span></code></pre></div><p><strong>Touches</strong>: <code>src/settings-tab.ts</code> (or <code>src/index.ts</code> if the descriptor is inline).</p><p><strong>What Claude should NOT touch manually after</strong>: the descriptor structure (Claude leaves it correct).</p><p><strong>What YOU should review</strong>:</p><ul><li>Is the label good in your language? If you have i18n, better pass it through <code>host.i18n.t(...)</code>.</li><li>If the toggle controls something in the game (not just in the mod), it should be <code>binding: &#39;tunables.X&#39;</code>, not <code>&#39;showToastOnGameOver&#39;</code>.</li></ul><p><strong>Typical risks</strong>:</p><ul><li>Claude may add a new permission &quot;just in case&quot; — verify <code>mod.json</code> that nothing was added you don&#39;t need.</li></ul><hr><h3 id="_1-2-—-i18n-translate-hardcoded-strings-to-locales" tabindex="-1">1.2 — i18n: translate hardcoded strings to <code>locales/</code> <a class="header-anchor" href="#_1-2-—-i18n-translate-hardcoded-strings-to-locales" aria-label="Permalink to &quot;1.2 — i18n: translate hardcoded strings to \`locales/\`&quot;">​</a></h3><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>In src/settings-tab.ts there are hardcoded strings in Spanish.</span></span>
<span class="line"><span>Make them i18n:</span></span>
<span class="line"><span></span></span>
<span class="line"><span>1. Create (or extend) locales/en.json and locales/es.json.</span></span>
<span class="line"><span>2. Use keys with namespace \`mod.&lt;modId&gt;.tab.&lt;area&gt;.&lt;element&gt;\`</span></span>
<span class="line"><span>   (read my mod.json for the modId).</span></span>
<span class="line"><span>3. In the code, replace the literal string with</span></span>
<span class="line"><span>   \`host.i18n?.t(&#39;mod.&lt;modId&gt;.tab.X.Y&#39;) ?? &#39;&lt;Spanish fallback&gt;&#39;\`.</span></span>
<span class="line"><span>4. If \`permissions[type=i18n]\` isn&#39;t declared in my mod.json,</span></span>
<span class="line"><span>   add it with correct namespace and concrete rationale.</span></span>
<span class="line"><span></span></span>
<span class="line"><span>DO NOT touch the keys that are ALREADY i18n-ized (the ones that</span></span>
<span class="line"><span>already use host.i18n.t). Only the hardcoded ones.</span></span></code></pre></div><p><strong>Touches</strong>: <code>locales/en.json</code>, <code>locales/es.json</code>, <code>src/settings-tab.ts</code> (or <code>src/index.ts</code>), possibly <code>mod.json</code>.</p><p><strong>What YOU should review</strong>:</p><ul><li><strong>English translations</strong>: Claude may leave something literal but imprecise. Read <code>en.json</code>.</li><li><strong><code>mod.json</code> <code>i18n</code> permission</strong>: if Claude added it, its <code>rationale</code> should explain what it translates (not &quot;For i18n&quot;).</li></ul><p><strong>Typical risks</strong>:</p><ul><li>Key outside declared namespace → the framework silently rejects it.</li><li>Spanish fallback in code + locale <code>es.json</code> also with the same string = redundancy (not a bug, but ugly). Pick one and be consistent.</li></ul><hr><h3 id="_1-3-—-refactor-n-inline-toggles-→-array-map-dry" tabindex="-1">1.3 — Refactor: N inline toggles → array + map (DRY) <a class="header-anchor" href="#_1-3-—-refactor-n-inline-toggles-→-array-map-dry" aria-label="Permalink to &quot;1.3 — Refactor: N inline toggles → array + map (DRY)&quot;">​</a></h3><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>In src/settings-tab.ts I have N inline entries \`{ kind:</span></span>
<span class="line"><span>&#39;toggle&#39;, label: &#39;...&#39;, binding: &#39;tunables.powerupXEnabled&#39; }\`,</span></span>
<span class="line"><span>all with the same shape. Refactor them to:</span></span>
<span class="line"><span></span></span>
<span class="line"><span>1. A TOGGLES array in src/toggles.ts with shape \`{ binding,</span></span>
<span class="line"><span>   i18nKey, fallback }\`.</span></span>
<span class="line"><span>2. In settings-tab.ts, \`.map(...)\` over the array to generate</span></span>
<span class="line"><span>   entries.</span></span>
<span class="line"><span>3. Keep the current ORDER (visually relevant).</span></span>
<span class="line"><span>4. Keep the i18n: if the current entries use \`host.i18n.t(...)\`,</span></span>
<span class="line"><span>   the array must carry \`i18nKey\` separate from \`fallback\`.</span></span>
<span class="line"><span></span></span>
<span class="line"><span>Reference pattern: studio.fun-config does exactly this — look at</span></span>
<span class="line"><span>game-mods/snake-classic/studio.fun-config/src/settings-tab.ts</span></span>
<span class="line"><span>if you need a reference.</span></span></code></pre></div><p><strong>Touches</strong>: <code>src/settings-tab.ts</code>, <strong>new</strong> <code>src/toggles.ts</code>.</p><p><strong>What YOU should review</strong>:</p><ul><li>The order must be preserved.</li><li>The bindings must match EXACTLY the previous ones (one typo and they stop applying to the game).</li></ul><p><strong>Typical risks</strong>: none critical — it&#39;s a mechanical refactor. If the build passes, it&#39;s fine.</p><hr><h3 id="_1-4-—-add-reset-to-defaults-button-applying-n-bindings" tabindex="-1">1.4 — Add &quot;Reset to defaults&quot; button applying N bindings <a class="header-anchor" href="#_1-4-—-add-reset-to-defaults-button-applying-n-bindings" aria-label="Permalink to &quot;1.4 — Add &quot;Reset to defaults&quot; button applying N bindings&quot;">​</a></h3><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>Add a button to the tab &quot;Restore default values&quot; that sets all</span></span>
<span class="line"><span>the toggles in the TOGGLES array (src/toggles.ts) to \`true\` (or</span></span>
<span class="line"><span>the default that comes as the second argument of the array).</span></span>
<span class="line"><span></span></span>
<span class="line"><span>Expected implementation:</span></span>
<span class="line"><span>1. Button in an &quot;Actions&quot; card at the end of the tab, variant</span></span>
<span class="line"><span>   &#39;ghost&#39;.</span></span>
<span class="line"><span>2. action: { kind: &#39;event&#39;, name: &#39;MYMOD_RESET_TOGGLES&#39; }.</span></span>
<span class="line"><span>3. Handler in src/index.ts that uses Promise.all to apply the N</span></span>
<span class="line"><span>   toggles with a single \`await\` (not serial — see recipe 2 of</span></span>
<span class="line"><span>   the cookbook if you need reference).</span></span>
<span class="line"><span>4. Persist the new values in storage so the UI reflects them on</span></span>
<span class="line"><span>   re-render.</span></span>
<span class="line"><span>5. Add &#39;MYMOD_RESET_TOGGLES&#39; to subscribe[] of the events</span></span>
<span class="line"><span>   permission in mod.json.</span></span>
<span class="line"><span>6. After applying, dispatch &#39;MOD_NOTIFICATION&#39; with text</span></span>
<span class="line"><span>   &quot;Values restored&quot; — add &#39;MOD_NOTIFICATION&#39; to dispatch[] of</span></span>
<span class="line"><span>   the events permission.</span></span></code></pre></div><p><strong>Touches</strong>: <code>src/settings-tab.ts</code>, <code>src/index.ts</code>, <code>mod.json</code>.</p><p><strong>What YOU should review</strong>:</p><ul><li><code>mod.json</code>: the <code>events</code> permission stayed coherent (subscribe <ul><li>dispatch both correct).</li></ul></li><li>The name <code>MYMOD_RESET_TOGGLES</code> — Claude may have invented another prefix. Convention: <code>MOD_&lt;your-modid-without-dots&gt;_&lt;action&gt;</code>.</li></ul><p><strong>Typical risks</strong>:</p><ul><li>Forgetting <code>await Promise.all</code> → 22 serial calls → perceptible UI freeze. Verify the code uses <code>Promise.all</code>.</li><li>If <code>host.storage.set</code> fails silently (quota exceeded), the UI doesn&#39;t reflect the reset. Check <code>result.ok</code> after each <code>set</code>.</li></ul><hr><h2 id="level-2-—-full-simple-mods-claude-proposes-modder-reviews" tabindex="-1">Level 2 — Full simple mods (Claude proposes, modder reviews) <a class="header-anchor" href="#level-2-—-full-simple-mods-claude-proposes-modder-reviews" aria-label="Permalink to &quot;Level 2 — Full simple mods (Claude proposes, modder reviews)&quot;">​</a></h2><h3 id="_2-1-—-final-score-notification-mod" tabindex="-1">2.1 — &quot;Final score notification&quot; mod <a class="header-anchor" href="#_2-1-—-final-score-notification-mod" aria-label="Permalink to &quot;2.1 — &quot;Final score notification&quot; mod&quot;">​</a></h3><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>Create a complete mod in this project that:</span></span>
<span class="line"><span></span></span>
<span class="line"><span>1. When the game fires GAME_OVER, shows an in-game toast</span></span>
<span class="line"><span>   notification with the final score.</span></span>
<span class="line"><span>2. Has a toggle in Settings → Mods → \`&lt;your tab&gt;\` that allows</span></span>
<span class="line"><span>   enabling/disabling the notification. Default: ON.</span></span>
<span class="line"><span>3. Persists the toggle between sessions.</span></span>
<span class="line"><span></span></span>
<span class="line"><span>Details:</span></span>
<span class="line"><span>- target.gameId: snake-classic. target.gameVersion: ^1.0.0.</span></span>
<span class="line"><span>- engine.preferred: quickjs-declarative-ui, fallback quickjs.</span></span>
<span class="line"><span>- Required permissions: settings-ui, events (subscribe</span></span>
<span class="line"><span>  GAME_OVER, dispatch MOD_NOTIFICATION), storage (quotaKb 16),</span></span>
<span class="line"><span>  i18n (namespace \`mod.&lt;your-modid&gt;\`).</span></span>
<span class="line"><span>- Each permission with concrete, NOT trivial rationale.</span></span>
<span class="line"><span>- i18n: locales/en.json + locales/es.json with the toggle</span></span>
<span class="line"><span>  string + the toast text.</span></span>
<span class="line"><span>- Edit mod.json with id, version 0.1.0, metadata.name, author.</span></span>
<span class="line"><span></span></span>
<span class="line"><span>DO NOT add dependencies to package.json. Only esbuild already</span></span>
<span class="line"><span>comes in devDeps.</span></span></code></pre></div><p><strong>Touches</strong>: <code>mod.json</code>, <code>src/index.ts</code>, <code>src/settings-tab.ts</code> (if you prefer), <code>locales/en.json</code>, <code>locales/es.json</code>.</p><p><strong>What YOU should review</strong>:</p><ul><li><strong><code>rationale</code> of permissions</strong>: the player reads them. If Claude left &quot;for notifications&quot;, rewrite to something concrete (&quot;Notifies the player with their final score at game over&quot;).</li><li><strong><code>metadata.author</code></strong>: Claude probably put &quot;Modder&quot; or similar. Put your real name or handle.</li><li><strong><code>id</code></strong>: convention <code>&lt;yourhandle&gt;.&lt;modname&gt;</code>. If Claude invented something, change it.</li><li><strong><code>GAME_OVER</code> event name</strong>: confirm in api-reference that it exists with that exact name.</li></ul><p><strong>Typical risks</strong>:</p><ul><li>Claude may invent an event name (<code>GAME_ENDED</code>, <code>MATCH_FINISHED</code>) when the real one is <code>GAME_OVER</code>. Confirm with api-reference.md or the game&#39;s code.</li></ul><hr><h3 id="_2-2-—-persisted-initial-speed-slider-mod" tabindex="-1">2.2 — &quot;Persisted initial-speed slider&quot; mod <a class="header-anchor" href="#_2-2-—-persisted-initial-speed-slider-mod" aria-label="Permalink to &quot;2.2 — &quot;Persisted initial-speed slider&quot; mod&quot;">​</a></h3><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>Create a complete mod that adds a slider to the Settings tab</span></span>
<span class="line"><span>controlling Snake&#39;s initial speed (tunable &#39;initialSpeedTickMs&#39;,</span></span>
<span class="line"><span>range 80-500ms, step 10, default 200).</span></span>
<span class="line"><span></span></span>
<span class="line"><span>Behavior:</span></span>
<span class="line"><span>- Moving the slider applies the change live AND persists in</span></span>
<span class="line"><span>  storage.</span></span>
<span class="line"><span>- On any game start (GAME_STARTED), re-apply the value from</span></span>
<span class="line"><span>  storage (defensive against game restarts).</span></span>
<span class="line"><span>- &quot;Restore default&quot; button under the slider that calls</span></span>
<span class="line"><span>  \`gameConfigReset\` and clears storage so the slider shows the</span></span>
<span class="line"><span>  game default.</span></span>
<span class="line"><span></span></span>
<span class="line"><span>Operational details:</span></span>
<span class="line"><span>- target.gameId: snake-classic.</span></span>
<span class="line"><span>- Permissions: settings-ui, game-specific (surface tunables,</span></span>
<span class="line"><span>  actions set+reset+snapshot), events (subscribe GAME_STARTED,</span></span>
<span class="line"><span>  subscribe MYMOD_RESET_SPEED), storage (16 KB), i18n</span></span>
<span class="line"><span>  (namespace \`mod.&lt;id&gt;\`).</span></span>
<span class="line"><span>- The slider must use \`binding: &#39;tunables.initialSpeedTickMs&#39;\`</span></span>
<span class="line"><span>  (with tunables. prefix) — the runtime applies the change</span></span>
<span class="line"><span>  automatically with no handler.</span></span>
<span class="line"><span>- The GAME_STARTED hook READS from storage and calls</span></span>
<span class="line"><span>  gameConfigSet — DOES NOT write storage (last-write-wins</span></span>
<span class="line"><span>  anti-pattern, see CLAUDE.md).</span></span>
<span class="line"><span></span></span>
<span class="line"><span>Take game-mods/snake-classic/studio.gameplay-tuner/ as reference</span></span>
<span class="line"><span>if you need to see the pattern in production.</span></span></code></pre></div><p><strong>Touches</strong>: <code>mod.json</code>, <code>src/index.ts</code>, <code>src/settings-tab.ts</code>, <code>locales/en.json</code> + <code>locales/es.json</code>.</p><p><strong>What YOU should review</strong>:</p><ul><li><strong><code>game-specific</code> permission</strong> — <code>surface: &#39;tunables&#39;</code>, <code>actions: [&#39;set&#39;, &#39;reset&#39;, &#39;snapshot&#39;]</code>. If Claude asked for other actions, question it.</li><li><strong><code>initialSpeedTickMs</code></strong> exists as a tunable in Snake — confirm in <code>tunables.ts</code> of the game or in api-reference.</li></ul><p><strong>Typical risks</strong>:</p><ul><li>Claude may mix the <code>tunables.</code> binding + a hook writing the same key → race condition. Verify the hook <strong>reads</strong> from storage and applies to the game with <code>callHostFn</code>, without calling <code>host.storage.set</code> for that key.</li></ul><hr><h3 id="_2-3-—-parametrizable-power-up-toggle-mod" tabindex="-1">2.3 — &quot;Parametrizable power-up toggle&quot; mod <a class="header-anchor" href="#_2-3-—-parametrizable-power-up-toggle-mod" aria-label="Permalink to &quot;2.3 — &quot;Parametrizable power-up toggle&quot; mod&quot;">​</a></h3><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>Create a parametrizable mod that toggles ON/OFF a specific</span></span>
<span class="line"><span>Snake power-up.</span></span>
<span class="line"><span></span></span>
<span class="line"><span>Mod parameter (configurable only by editing a TYPESCRIPT</span></span>
<span class="line"><span>constant in src/, NOT via runtime UI):</span></span>
<span class="line"><span></span></span>
<span class="line"><span>\`\`\`ts</span></span>
<span class="line"><span>const POWER_UP: string = &#39;speedBoost&#39;;  // editable</span></span>
<span class="line"><span>\`\`\`</span></span>
<span class="line"><span></span></span>
<span class="line"><span>Behavior:</span></span>
<span class="line"><span>- Generates a tab with ONE single toggle \`Enable {PowerUp Name}\`.</span></span>
<span class="line"><span>- Default: true (same as the game&#39;s default).</span></span>
<span class="line"><span>- Moving the toggle calls</span></span>
<span class="line"><span>  \`gameConfigSet(&#39;powerup{POWER_UP}Enabled&#39;, bool)\`.</span></span>
<span class="line"><span>- Persists the last state with binding</span></span>
<span class="line"><span>  &#39;tunables.powerup...Enabled&#39;.</span></span>
<span class="line"><span></span></span>
<span class="line"><span>If POWER_UP is not one of the 22 valid ones, the mod logs warn</span></span>
<span class="line"><span>and doesn&#39;t register the tab.</span></span>
<span class="line"><span></span></span>
<span class="line"><span>Valid POWER_UP list: speedBoost, invincibility, doublePoints,</span></span>
<span class="line"><span>magnet, shrink, ghost, goldenApple, demolition, earthquake,</span></span>
<span class="line"><span>bombPickup, brickBlast, extraLife, summonSnake, blindfold,</span></span>
<span class="line"><span>fragileWall, brickRevival, portal, demon, baseballBat,</span></span>
<span class="line"><span>doubleLength, rainbowHeart, timeTravel.</span></span></code></pre></div><p><strong>Touches</strong>: <code>mod.json</code>, <code>src/index.ts</code>, <code>src/settings-tab.ts</code>, <code>locales/en.json</code> + <code>locales/es.json</code>.</p><p><strong>What YOU should review</strong>:</p><ul><li><strong>Constant validation</strong>: that the code actually logs warn + skip if the value is invalid. If it doesn&#39;t, demand fail-fast.</li><li><strong>Name i18n</strong>: that the toggle label uses <code>host.i18n.t(...)</code> and not the internal camelCase (<code>speedBoost</code> as a label is ugly; it should be &quot;Speed Boost&quot;).</li></ul><hr><h3 id="_2-4-—-easy-normal-hard-preset-dropdown-mod" tabindex="-1">2.4 — &quot;Easy/Normal/Hard preset dropdown&quot; mod <a class="header-anchor" href="#_2-4-—-easy-normal-hard-preset-dropdown-mod" aria-label="Permalink to &quot;2.4 — &quot;Easy/Normal/Hard preset dropdown&quot; mod&quot;">​</a></h3><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>Create a complete mod that adds a dropdown (&quot;select&quot;) to the</span></span>
<span class="line"><span>tab with 3 options: Easy / Normal / Hard. Applying the preset</span></span>
<span class="line"><span>applies several game tunables at once.</span></span>
<span class="line"><span></span></span>
<span class="line"><span>Presets (you can adjust — sensible but edit freely):</span></span>
<span class="line"><span></span></span>
<span class="line"><span>- Easy: maxLives=5, initialSpeedTickMs=300, pointsPerFood=10.</span></span>
<span class="line"><span>- Normal: maxLives=3, initialSpeedTickMs=200, pointsPerFood=15.</span></span>
<span class="line"><span>- Hard: maxLives=1, initialSpeedTickMs=120, pointsPerFood=25.</span></span>
<span class="line"><span></span></span>
<span class="line"><span>Details:</span></span>
<span class="line"><span>- The dropdown uses \`kind: &#39;select&#39;\` with</span></span>
<span class="line"><span>  \`binding: &#39;mymod.preset&#39;\`.</span></span>
<span class="line"><span>- An &quot;Apply preset&quot; button below fires the</span></span>
<span class="line"><span>  MYMOD_APPLY_PRESET event with payload { preset:</span></span>
<span class="line"><span>  &#39;easy&#39;|&#39;normal&#39;|&#39;hard&#39; }.</span></span>
<span class="line"><span>- The handler applies the 3 tunables IN PARALLEL with</span></span>
<span class="line"><span>  Promise.all + persists each one in storage so the UI</span></span>
<span class="line"><span>  reflects them (if there are other sliders in the tab —</span></span>
<span class="line"><span>  optional for this version).</span></span>
<span class="line"><span>- After applying, dispatch MOD_NOTIFICATION with &quot;Preset X</span></span>
<span class="line"><span>  applied&quot;.</span></span>
<span class="line"><span></span></span>
<span class="line"><span>Reference: studio.gameplay-tuner uses this exact pattern — look</span></span>
<span class="line"><span>at game-mods/snake-classic/studio.gameplay-tuner/src/presets.ts</span></span>
<span class="line"><span>and src/apply-config.ts if you need the shape.</span></span></code></pre></div><p><strong>Touches</strong>: <code>mod.json</code>, <code>src/index.ts</code>, <code>src/settings-tab.ts</code>, <code>src/presets.ts</code> (new), <code>locales/en.json</code> + <code>locales/es.json</code>.</p><p><strong>What YOU should review</strong>:</p><ul><li>The preset values — Easy with 5 lives is reasonable, but the <code>pointsPerFood</code> are your decision. Edit if you want.</li><li>The event name <code>MYMOD_APPLY_PRESET</code> — convention <code>MOD_&lt;your-modid-without-dots&gt;_&lt;verb&gt;</code>.</li></ul><p><strong>Typical risks</strong>:</p><ul><li><strong>Missing <code>Promise.all</code></strong> → 3 serial calls → ~3ms perceptible. Verify the code uses it.</li><li><strong><code>maxLives</code>/<code>initialSpeedTickMs</code>/<code>pointsPerFood</code></strong> — all are valid tunables in Snake, but confirm in the game&#39;s <code>tunables.ts</code>.</li></ul><hr><h2 id="level-3-—-release-tasks-boilerplate" tabindex="-1">Level 3 — Release tasks (boilerplate) <a class="header-anchor" href="#level-3-—-release-tasks-boilerplate" aria-label="Permalink to &quot;Level 3 — Release tasks (boilerplate)&quot;">​</a></h2><h3 id="_3-1-—-generate-icon-256x256-3-descriptive-screenshots" tabindex="-1">3.1 — Generate icon 256x256 + 3 descriptive screenshots <a class="header-anchor" href="#_3-1-—-generate-icon-256x256-3-descriptive-screenshots" aria-label="Permalink to &quot;3.1 — Generate icon 256x256 + 3 descriptive screenshots&quot;">​</a></h3><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>Your mod does [BRIEF 1-SENTENCE DESCRIPTION]. I need:</span></span>
<span class="line"><span></span></span>
<span class="line"><span>1. icon.png 256x256 with optional transparency, &lt;50KB,</span></span>
<span class="line"><span>   recognizable at 64x64. Style coherent with [Snake / pixel</span></span>
<span class="line"><span>   art / minimalist — pick].</span></span>
<span class="line"><span>2. 3 screenshots 1920x1080 for Workshop:</span></span>
<span class="line"><span>   - screenshot 1: the mod tab in Settings with its controls.</span></span>
<span class="line"><span>   - screenshot 2: the effect of the mod in the game (visible</span></span>
<span class="line"><span>     change).</span></span>
<span class="line"><span>   - screenshot 3: optional, description/diagram if the mod is</span></span>
<span class="line"><span>     conceptually complex.</span></span>
<span class="line"><span></span></span>
<span class="line"><span>For the screenshots, suggest what to capture exactly step by</span></span>
<span class="line"><span>step (I&#39;ll do it with cmd-shift-4 or equivalent, you don&#39;t have</span></span>
<span class="line"><span>to generate them).</span></span>
<span class="line"><span></span></span>
<span class="line"><span>For the icon, propose 3 prompt variants for the IDE&#39;s image gen</span></span>
<span class="line"><span>(Anthropic Studio, DALL-E, Stable Diffusion) I can choose from.</span></span></code></pre></div><p><strong>Touches</strong>: nothing in code. Output is image gen prompt + manual QA suggestions.</p><p><strong>What YOU should review</strong>: the icon must be legible at small size (64x64 in the mod list). Variants with text are usually unreadable — prefer symbols.</p><hr><h3 id="_3-2-—-generate-changelog-for-v0-2-0" tabindex="-1">3.2 — Generate changelog for v0.2.0 <a class="header-anchor" href="#_3-2-—-generate-changelog-for-v0-2-0" aria-label="Permalink to &quot;3.2 — Generate changelog for v0.2.0&quot;">​</a></h3><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>I&#39;m about to publish v0.2.0 of my mod. The previous version is</span></span>
<span class="line"><span>v0.1.0.</span></span>
<span class="line"><span></span></span>
<span class="line"><span>Read \`git log v0.1.0..HEAD --oneline\` (or \`git log --since</span></span>
<span class="line"><span>&quot;v0.1.0&quot;\` if there&#39;s no tag) and generate:</span></span>
<span class="line"><span></span></span>
<span class="line"><span>1. CHANGELOG.md (or append to existing CHANGELOG at the top):</span></span></code></pre></div><h2 id="v0-2-0-yyyy-mm-dd" tabindex="-1">v0.2.0 - YYYY-MM-DD <a class="header-anchor" href="#v0-2-0-yyyy-mm-dd" aria-label="Permalink to &quot;v0.2.0 - YYYY-MM-DD&quot;">​</a></h2><h3 id="added" tabindex="-1">Added <a class="header-anchor" href="#added" aria-label="Permalink to &quot;Added&quot;">​</a></h3><ul><li>...</li></ul><h3 id="changed" tabindex="-1">Changed <a class="header-anchor" href="#changed" aria-label="Permalink to &quot;Changed&quot;">​</a></h3><ul><li>...</li></ul><h3 id="fixed" tabindex="-1">Fixed <a class="header-anchor" href="#fixed" aria-label="Permalink to &quot;Fixed&quot;">​</a></h3><ul><li>...</li></ul><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span></span></span>
<span class="line"><span>2. &quot;Steam Workshop description&quot; version (short markdown, max</span></span>
<span class="line"><span>500 chars) ready to paste to the item&#39;s page on Update Item.</span></span>
<span class="line"><span></span></span>
<span class="line"><span>Bump convention:</span></span>
<span class="line"><span>- patch (0.1.0 → 0.1.1): only bug fixes.</span></span>
<span class="line"><span>- minor (0.1.0 → 0.2.0): non-breaking features. Settings</span></span>
<span class="line"><span>preserved on update.</span></span>
<span class="line"><span>- major (0.1.0 → 1.0.0): breaks. **Settings RESET</span></span>
<span class="line"><span>automatically on update.**</span></span>
<span class="line"><span></span></span>
<span class="line"><span>If the commits indicate something that breaks (binding rename,</span></span>
<span class="line"><span>change in mod.json#permissions), warn me before generating —</span></span>
<span class="line"><span>maybe it should be 1.0.0 instead of 0.2.0.</span></span></code></pre></div><p><strong>Touches</strong>: <code>CHANGELOG.md</code> (new or append).</p><p><strong>What YOU should review</strong>:</p><ul><li>If Claude detected a breaking change, <strong>trust it</strong> — evaluate if MAJOR is correct. Resetting player settings is not trivial.</li><li>The &quot;Steam Workshop description&quot; gets short on details easily. If you want it detailed, refine manually.</li></ul><hr><h3 id="_3-3-—-generate-mod-readme-md" tabindex="-1">3.3 — Generate mod README.md <a class="header-anchor" href="#_3-3-—-generate-mod-readme-md" aria-label="Permalink to &quot;3.3 — Generate mod README.md&quot;">​</a></h3><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>Generate a professional README.md for my mod, reading mod.json</span></span>
<span class="line"><span>and src/. Structure:</span></span>
<span class="line"><span></span></span>
<span class="line"><span># {metadata.name}</span></span>
<span class="line"><span></span></span>
<span class="line"><span>[Badge \`mod for Snake Classic\`] [Badge \`v0.1.0\`] [Badge \`MIT\`]</span></span>
<span class="line"><span></span></span>
<span class="line"><span>{metadata.description in 1-2 sentences}</span></span>
<span class="line"><span></span></span>
<span class="line"><span>## What it does</span></span>
<span class="line"><span></span></span>
<span class="line"><span>[3-5 bullets describing each feature, pulling from the code]</span></span>
<span class="line"><span></span></span>
<span class="line"><span>## Screenshots</span></span>
<span class="line"><span></span></span>
<span class="line"><span>![](screenshots/01.png)</span></span>
<span class="line"><span>![](screenshots/02.png)</span></span>
<span class="line"><span></span></span>
<span class="line"><span>## Install</span></span>
<span class="line"><span></span></span>
<span class="line"><span>### Via Steam Workshop</span></span>
<span class="line"><span></span></span>
<span class="line"><span>1. Subscribe at {link}.</span></span>
<span class="line"><span>2. Open Snake Classic. The mod appears in Settings → Mods.</span></span>
<span class="line"><span></span></span>
<span class="line"><span>### Manual sideload</span></span>
<span class="line"><span></span></span>
<span class="line"><span>\`\`\`bash</span></span>
<span class="line"><span>git clone {repo}</span></span>
<span class="line"><span>pnpm install &amp;&amp; pnpm build</span></span>
<span class="line"><span># Copy to userData/snake-classic/mods/{modId}/</span></span>
<span class="line"><span>\`\`\`</span></span>
<span class="line"><span></span></span>
<span class="line"><span>## Permissions</span></span>
<span class="line"><span></span></span>
<span class="line"><span>List each permission from mod.json with its rationale,</span></span>
<span class="line"><span>formatted as a table.</span></span>
<span class="line"><span></span></span>
<span class="line"><span>## Credits</span></span>
<span class="line"><span></span></span>
<span class="line"><span>- Author: {metadata.author}</span></span>
<span class="line"><span>- License: {metadata.license}</span></span>
<span class="line"><span>- Built with Snake Classic mod framework</span></span>
<span class="line"><span>  (https://leteoworks.github.io/mod-portal-snake-classic).</span></span>
<span class="line"><span></span></span>
<span class="line"><span>## Changelog</span></span>
<span class="line"><span></span></span>
<span class="line"><span>See CHANGELOG.md.</span></span>
<span class="line"><span></span></span>
<span class="line"><span>DO NOT invent features not in the code. If a section doesn&#39;t</span></span>
<span class="line"><span>apply, omit it.</span></span></code></pre></div><p><strong>Touches</strong>: <code>README.md</code> (overwrite if exists).</p><p><strong>What YOU should review</strong>:</p><ul><li>The <code>repo URL</code> for the Manual sideload section — Claude doesn&#39;t know it. Edit it.</li><li>The Steam Workshop <code>&lt;link&gt;</code> — placeholder. Edit it when you have the Workshop ID.</li></ul><hr><h2 id="how-to-add-prompts-to-this-gallery" tabindex="-1">How to add prompts to this gallery <a class="header-anchor" href="#how-to-add-prompts-to-this-gallery" aria-label="Permalink to &quot;How to add prompts to this gallery&quot;">​</a></h2><p>If you have a tested prompt that works well and isn&#39;t here, open a PR against <a href="https://github.com/leteoworks/mod-portal-snake-classic" target="_blank" rel="noreferrer"><code>leteoworks/mod-portal-snake-classic</code></a> adding an entry in this <code>example-prompts.md</code>. Include:</p><ul><li>Copy-paste prompt text.</li><li>Level (1/2/3).</li><li>Which files it touches.</li><li>What to review manually after.</li><li>Typical risks and how to detect them.</li></ul><p>We keep the gallery short and quality-focused — better 10 tested prompts than 50 hallucinated ones.</p>`,99)])])}const u=a(p,[["render",l]]);export{g as __pageData,u as default};
